# Large data files
restaurant-sim-batch-*.json
restaurant-sim-merged.json
ml/restaurant-sim-batch-*.json
ml/restaurant-sim-merged.json

# Trained model artifacts (large binaries)
surrogate_model.pkl
gnn_model.pt
ml/surrogate_model.pkl
ml/gnn_model.pt

# Node
node_modules/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# Generated output (regenerate with scripts in ml/)
validation-results.json
optimizer-results.json
ml/validation-results.json
ml/optimizer-results.json
ml/_batch_layouts.json
ml/_batch_validated.json

# Internal AI tool config
CLAUDE.md

# Intermediate validation artifacts
best-layout-validate.json
best-new-validate.json

# OS
.DS_Store
Thumbs.db

# Interne AI/tool-config
.mcp.json

# Out-of-fold voorspellingen (regenereer met ml/train_surrogate.py)
surrogate-oof.json
ml/surrogate-oof.json

# Verzamelde shards (regenereer met ml/collect_parallel.py)
data/shards/
restaurant-sim-clean.json
logs/

# Gearchiveerde shards en datasets van eerdere verzamelrondes.
# Samen ~568 MB; `git add -A` zou ze anders meenemen.
data/shards-*/
restaurant-sim-clean.json.*
*.bak-*
data/exp-label-noise-oof.*
data/room-pool.json

# Losse werkmap, hoort niet in de repo
De competitie/
