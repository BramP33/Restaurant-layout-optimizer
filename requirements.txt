# Kern-pipeline (train_surrogate.py, optimize_layout.py, evaluate.py)
numpy>=2.0
scipy>=1.11
scikit-learn>=1.5
xgboost>=2.0
joblib>=1.3

# Alleen nodig voor de GNN (train_gnn.py, gnn_layout.py) — fase 3 van het plan:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install torch-geometric
